[user]
	name = Radu Cotescu
	email = {{ .email }}
{{- if and (eq .chezmoi.os "darwin") .work }}
	signingkey = {{ onepasswordRead "op://Adobe/ldoe4gail3rnpfrzynhxbjg4my/public key" | trim }}
{{ else if and (eq .chezmoi.os "darwin") .personal }}
	signingkey = {{ onepasswordRead "op://Private/Private Ed25519 SSH Key/public key" | trim }}
{{ end }}
[core]
	pager = delta
	editor = vim
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
[mergetool "sourcetree"]
	cmd = /Applications/Sourcetree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[commit]
{{- if eq .chezmoi.os "darwin" }}
	gpgsign = true
{{- else }}
	gpgsign = false
{{- end }}
[init]
	defaultBranch = main
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[gpg]
{{- if eq .chezmoi.os "darwin" }}
	format = ssh
[gpg "ssh"]
	program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign
{{- end }}
[color]
	ui = auto
[interactive]
    diffFilter = delta --color-only

[pager]
    blame = delta

[delta]
    navigate = true  # use n and N to move between diff sections
    side-by-side = true
    hyperlinks = true
    true-color = always
[credential]
	helper = /usr/local/share/gcm-core/git-credential-manager

{{- if .work }}
[url "git@ghec.com:OneAdobe"]
	insteadOf = git@github.com:OneAdobe
[credential "https://gitbox.apache.org"]
	provider = generic
[credential "https://git.cloudmanager.adobe.com"]
	useHttpPath = true
[credential "https://dev.azure.com"]
	useHttpPath = true
{{- end }}
